name: CI

on: [push]

jobs:
  build:

    runs-on: [ubuntu-18.04]

    steps:
    - uses: actions/checkout@v1
    - name: Yarn Install
      run: yarn install --pure-lockfile
    - name: Test
      run: yarn test
